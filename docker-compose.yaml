version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://localhost:8080/api
    container_name: food-dispatch-frontend
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: http://localhost:8080/api
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000', (r) => {if (r.statusCode !== 200) throw new Error(r.statusCode)})"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s

  # Versión de desarrollo (comentada)
  # frontend-dev:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: food-dispatch-frontend-dev
  #   ports:
  #     - "9002:9002"
  #   environment:
  #     NODE_ENV: development
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
  #   command: npm run dev
  #   restart: unless-stopped

volumes: {}
networks:
  default:
    name: food-dispatch-network
